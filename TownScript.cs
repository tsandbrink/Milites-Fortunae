using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TownScript : MonoBehaviour
{

    public Unit Saracene;
    public Unit Archer;
    public SelectUnits rosterManager;
    
    // Start is called before the first frame update
    void Start()
    {
        rosterManager.Clear();
    }

  
    
}
